structure pane
scalarを選択した後に選択肢(string,char,number)がinput select形式になってるけどチェックボックス形式で選択できるように(他の構造がある箇所(arrayとか)も同様)
実数を入れるところもその並びにあるけど、そこだけselectがformのフィールドに鳴っている感じで。
一度入れたstructureも編集できるように

constraint pane
一度入れたconstraintも編集できるように
ポップアップが開いているときに数値を入れた後わざわざエンター押下しなくてもフォームからfocusが離れられるようにして。lower入力→upper入力がスムーズにできるように。あと、入れるべきところを全部入力し終わったらsubmitできる感じ

あと、関数適用のところだけど、使用した結果以下のようなものを入れるとsampleが出ません。
原因も調査して根本対処だ。このコーナーケースだけに対応するような対応じゃダメよ
●1 ≤ n ≤ 4
●1 ≤ A ≤ 10^n

以下も
●1 ≤ N ≤ 100
●1 ≤ A ≤ N

多分制約に他の変数を含んでいる場合のトポソがちゃんとできてない？
原因調査と対応をお願い

あと、Nのupper lowerの選択肢にAが出るのはおかしい(Aは配列)
Nのupper lowerの選択肢に自分自身(N)が出るのもおかしいよ


あと、この制約の対象がnumberではなくてもupper lowerが出るのもおかしい
文字であるならば取りうる文字の制約を一個づつ入れる

例 ( )のみの例
まずvariant のinputフィールドがあってそこに(を入れる、次に、input fieldの→に +アイコンがあるのでそれをクリックするとinputフィールドが追加されるのでそこに)を入れる。
そしてsubmit
文字列の場合も同様だが、こっちはlower とupperはいる

あとstructure paneのsequenceで下に追加するやつは固定位置じゃなくて今あるやつの一番下にある要素の下に追加すべき。

propertyから追加したtreeのような制約は追加した後もconstraintの右にバッテンマークを入れておいてワンクリックで消せるようにしてくれ

あとsp対応もしてくれ。
spは横ではなく縦にpaneをリストにする形式でそれぞれトグルで折り畳める感じ。

あとgithub pagesとしてこれを公開できるようにもしておいて
以上だ

